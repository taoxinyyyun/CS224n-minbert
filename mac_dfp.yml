name: mac_dfp
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.8
  - pytorch
  - torchvision
  - torchaudio
  #- pytorch-cuda
  - pip
  - pip:
    - tqdm
    - requests
    - importlib-metadata
    - filelock
    - scikit-learn
    - tokenizers
    - explainaboard_client